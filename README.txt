These are a few configurations files that suitted for raspberry pi cluster. 

Also, it contains the Ansible playbook to distribute certain configuration file to certain nodes. 

Issues:
(1) when the yarn service starts, it is super slow, probably need to look at properties to make sure the default hadoop value won't overload raspberrypi.
(2) to-do 
